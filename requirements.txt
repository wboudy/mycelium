# Mycelium - Obsidian-compatible knowledge vault with ingestion pipeline

# YAML frontmatter parsing and serialization
pyyaml>=6.0

# MCP server (tool exposure for external agents)
fastmcp>=2.0

# LLM provider abstraction (agent orchestration)
litellm>=1.0

# Development tools
ruff>=0.1.0
pytest>=7.0.0
